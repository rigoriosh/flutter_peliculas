

import 'package:flutter/material.dart';

class PaginaDirectios extends StatelessWidget {
  const PaginaDirectios({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("pagina direciones"),);
  }
}